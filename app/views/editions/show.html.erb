<div class="event-card col-8 mx-auto mt-5 p-4">
  <div class="event-slide d-flex flex-column justify-content-between">
    <h2><%= @edition.name %></h2>
    <div class="">

      <% if @edition.pending? %>
        <%= render partial: 'editions/show-editions/pending-show' %>
      <% elsif @edition.active? %>
        <%= render partial: 'editions/show-editions/active-show' %>
      <% elsif @edition.archived? %>
        <%= render partial: 'editions/show-editions/archive-show' %>
      <% end %>
      <%= link_to 'Edit', edit_event_edition_path(@edition) %>
    </div>
  </div>
</div>

<%= render partial: 'editions/calendar' %>


<!-- if pending -->
  <!-- name -->
  <!-- guidelines -->
  <!-- guests -->
  <!-- host name -->
  <!-- surveys -->
      <!-- notes -->
      <!-- edit edition -->


<!-- if active -->
  <!-- name -->
  <!-- guidelines -->
  <!-- guests -->
  <!-- host name -->
  <!-- start time -->
  <!-- end time -->
  <!-- start_address -->
  <!-- map -->
  <!-- items -->
  <!-- countdown -->
      <!-- address -->
      <!-- notes -->


<!-- if past -->
  <!-- name -->
  <!-- guidelines -->
  <!-- guests -->
  <!-- host name -->
  <!-- host rating -->
  <!-- start time -->
  <!-- end time -->
  <!-- address -->
  <!-- map -->
  <!-- rating -->
