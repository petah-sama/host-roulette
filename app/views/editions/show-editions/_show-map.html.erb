<% if @edition.address.present? %>
  <div
    id="map"
    class="show-map"
    style="width: 100%; height: 240px;"
    data-markers="<%= @markers.to_json %>"
    data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
<% end %>
