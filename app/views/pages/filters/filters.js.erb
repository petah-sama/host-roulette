/*const filterActive = document.getElementById('filter-by-active');
const filterHost = document.getElementById('filter-by-host')


filterActive.addEventListener(click, (event) => {
  event.currentTarget.classList.toggle("hiding-active")
})

filterHost.addEventListener(click, (event) => {
  event.currentTarget.classList.toggle("hiding-active")
})
*/
